export module spectra.scene.assets;

import spectra.scene;
import std;

namespace spectra::scene {
    export void load_scene_sources(Scene& scene, const std::filesystem::path& root);
} // namespace spectra::scene
